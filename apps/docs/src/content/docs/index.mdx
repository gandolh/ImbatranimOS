---
title: ImbatranimOS
description: A real little computer whose screen is a browser tab.
template: splash
hero:
  tagline: One <code>docker run</code>. A real Alpine Linux userland — its own filesystem, shell, and process table — with a React desktop as its screen.
  actions:
    - text: Take the tour
      link: /tour/
      icon: right-arrow
      variant: primary
    - text: The ideas behind it
      link: /ideas/
      icon: open-book
      variant: secondary
    - text: Architecture
      link: /architecture/
      icon: document
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components'
import Metrics from '../../components/Metrics.astro'

<Metrics
  items={[
    { v: '385', unit: 'MB', k: 'image size' },
    { v: '1.42', unit: 's', k: 'cold start' },
    { v: '41', unit: 'MiB', k: 'idle RAM' },
    { v: '23', k: 'desktop apps' },
    { v: '135', k: 'backend tests' },
  ]}
/>

## The ideas, in one breath

<CardGrid>
  <Card title="Real, not simulated" icon="laptop">
    Terminal = a real PTY. Files = the real FS. Monitor = real `/proc`. Faking the system is off-soul.
  </Card>
  <Card title="One container, one port" icon="seti:docker">
    Alpine + Node serve the desktop and API on one authenticated port. The server streams **data**, never pixels.
  </Card>
  <Card title="No sudo, ever" icon="approve-check">
    Runs as the `imbatranim` user. argon2id password, optional TOTP, internet-exposable behind a proxy.
  </Card>
  <Card title="Lightweight is identity" icon="rocket">
    A slim image and a snappy desktop. A new dependency needs a reason.
  </Card>
</CardGrid>

The [full six](/ideas/) — and the matching kill-list of what this project
*refuses* to build — are what every decision is measured against.

## Start here

<CardGrid>
  <Card title="A tour of the desktop" icon="laptop">
    The [23 apps](/tour/) and platform surfaces — what you can actually *do* on
    the machine, from a real shell to a client-side spreadsheet.
  </Card>
  <Card title="The ideas that empower it" icon="open-book">
    The [six load-bearing ideas](/ideas/) — real-not-simulated, data-not-pixels,
    the versioned seam — that give the project its soul.
  </Card>
  <Card title="Patterns & techniques" icon="puzzle">
    The [seven recurring moves](/patterns/) — the seam, the FS jail, the bounded
    proxy, lazy chunks, virtualization — with the numbers behind each.
  </Card>
  <Card title="The technology" icon="seti:typescript">
    The [whole stack](/stack/), layer by layer, with the reason each piece was
    chosen — and each one it beat.
  </Card>
</CardGrid>

## Kept honest by construction

<CardGrid>
  <Card title="Narrative — rendered from the corpus" icon="document">
    The deep-dive pages — [architecture](/wiki/architecture/),
    [decisions](/wiki/decisions/), [status](/wiki/status/) — render straight from
    the project's [`corpus/`](/wiki/overview/) wiki. Intent is authored, never
    reverse-engineered.
  </Card>
  <Card title="Reference — generated from the code" icon="seti:typescript">
    The [API reference](/reference/) is generated from the TypeScript every build —
    Compodoc (backend) + TypeDoc (core). It can't drift.
  </Card>
</CardGrid>
